# Netlify Configuration - v37.2 [ULTRA-COMPATIBLE]
# Aceasta versiune este simplificata la maxim pentru a trece de eroarea "noble".

[build]
  functions = "netlify/functions"
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
